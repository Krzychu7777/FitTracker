@model IEnumerable<FitTracker.Models.ExercisesList>

<head>
    <link rel="stylesheet" href="~/css/exercisesList.css" asp-append-version="true" />
</head>

<div class="exercises-list-wrapper">
    <h1>Lista ćwiczeń</h1>

    <div class="add-button-wrapper">
        <a asp-action="Create">Dodaj nowy</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BurnedCalories)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TrainingAt)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BurnedCalories)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TrainingAt)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edytuj</a>
                        <a asp-action="Details" asp-route-id="@item.Id">Szczegóły</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="delete">Usuń</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
